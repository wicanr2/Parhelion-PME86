# 術語表

同一個東西在手冊、直譯器標籤、本 repo 之間可能有三個名字。這裡定一個。

## p-machine 的東西

| 詞 | 意思 |
|---|---|
| **p-code** | UCSD Pascal 編譯出來的位元組碼。一個位元組的 opcode 加零到三個運算元。 |
| **p-machine** | 執行 p-code 的抽象堆疊機。手冊 p.13 定義它的暫存器組。 |
| **PME** | P-Machine Emulator，直譯器本身。`SYSTEM.PME.86` 是 8086 版。 |
| **dispatch 表** | 256 項的位址表，用 opcode 索引。 |
| **big operand** | 變長運算元。最高位為 0 是 0–127，為 1 則兩個位元組表示 0–32767。 |
| **Mark Stack / MSCW** | 活動記錄開頭的控制欄位：`MSSTAT`、`MSDYN`、`MSIPC`、`MSENV`、`MSPROC`，五個 word。 |
| **MP** | Mark Pointer，指向目前活動記錄的 `MSSTAT`。 |
| **靜態鏈 / 動態鏈** | `MSSTAT` 指語彙上外一層，`MSDYN` 指呼叫者。 |
| **segment** | 可獨立換入換出的程式碼單位。跨段呼叫是 `CXL`／`CXG`／`CXI`。 |
| **E_Rec** | 段的執行期描述子。8086 版的欄位是 `Env_Data`（+0）、`E_Vec`（+2）、`SIB`（+4）。 |
| **E_Vec** | 以 segment number 索引的 `E_Rec` 指標陣列。 |
| **SIB** | Segment Information Block。放段在記憶體的位址、參考計數、活動度。 |
| **Codepool** | 段被換入時放的記憶體區。本 repo 的 remake 不打算做。 |
| **常數池** | 段內存放常數的區域，`LCO`／`LDC`／`XJP` 從這裡取值。 |
| **程序字典** | 段內把程序號對到程序碼位址的表，**往回長**。 |
| **byte sex** | 段的位元組序是否與主機相同。`MOV`、`LDC`、`XJP` 會依它決定要不要交換位元組。 |
| **packed field** | `packed` 型別的位元欄位。位址是堆疊上三個 word：位址、位元數、最右 bit 編號。 |
| **CSP** | 在 IV.0 是 `Copy String Parameter`（opcode 172）。**不要**當成「呼叫標準程序」的縮寫。 |

## 這份實作的東西

| 詞 | 意思 |
|---|---|
| **inline threading** | 分派碼複製到每支常式的結尾，沒有共同主迴圈。8086 版採用；68000 版不是。 |
| **局部資料基底** | `MP + 8`，常駐在 `bx`。變數編號 n 在 `bx + 2n`。 |
| **全域資料基底** | `Env_Data + 8`，常駐在 `dx`，記憶體副本在 `ss:28h`。 |
| **內嵌程序表** | `0x1f56` 起 48 格，`CXG` 呼叫 segment 1 時先查這裡；33 格非零。 |
| **遮罩表** | `0x1fb6` 起 17 格，第 n 項是 `(1 << n) − 1`。`LDP`／`STP`／`SRS` 用。 |

## remake 的東西

| 詞 | 意思 |
|---|---|
| **oracle** | 原版直譯器在 DOSBox 裡跑出來的行為，用來與 Go 版對拍。 |
| **spec 狀態** | `DRAFT`（未配證據）、`READY`（可實作）、`CONFORMED`（已同狀態驗證）。 |
| **remake 自訂** | 原版沒有對應物的決定。要與「刻意不照做」分開標。 |
| **刻意不照做** | 原版有這個行為，remake 決定不要（例如段換出）。 |

## 寫法約定

- 檔案偏移一律寫 `0x` 開頭的小寫十六進位，指的是**檔案偏移**不是執行期位址。
- opcode 同時給十進位與十六進位時，以手冊的十進位為準。
- 引用手冊寫「手冊 p.NN」，頁碼是**印刷頁碼**。
