# 進度與待辦

## 分輪紀錄

### R1（2026-09-05）建立 repo，第一批九篇

- 用擴充後的 `tools/dump-routines-86.py` 重跑 IDA：**169 支 dispatch 目標全部反組譯成功，
  另外追出 25 支助手常式**（追 `call` 目標）。輸入 sha256 `fe427aa66ca8…`，
  截斷上限 80 條，只有 `MPR`（`0x24e1`）與 `DVR`（`0x262a`）觸頂。
- 寫成四篇：[分派](docs/10-interpreter/dispatch-and-threading.md)、
  [狀態](docs/10-interpreter/machine-state.md)、
  [定址](docs/10-interpreter/addressing.md)、
  [段切換](docs/10-interpreter/segment-switching.md)，
  加一份工具產生的 [256 格對照表](docs/10-interpreter/opcode-map.md)。
- 格式兩篇：[`.VOL`](docs/20-formats/vol-image.md)、
  [檔頭](docs/20-formats/pme86-header.md)。
- 重做兩篇：[可行性評估](docs/30-remake/feasibility.md)、
  [spec 閘門](docs/30-remake/spec-workflow.md)。

這一輪新解出來的東西：

| 結論 | 證據 |
|---|---|
| 分派碼 8 個位元組內嵌在 129 支常式結尾；另有省一個位元組的變體用了 50 支 | 比對結尾五條指令的位元組 |
| `bx` = 局部資料基底 = `MP + 8`；`dx` = 全域資料基底 = `Env_Data + 8` | 助手 `0x093e`、`LAO`、`RPU` `0x114e`、`DVI`／`STP` 的 `mov dx, ss:28h` |
| 變數編號 1 落在 `MP + 10`，正好是 MSCW 之後第一個 word | `RPU` 逐格彈出 MSCW 五個欄位 |
| `ss:42h` 常數池基底、`ss:44h` byte sex、`ss:24h` 存起來的 IPC | `LCO`／`LDC`／`XJP`；`MOV` 的交換位元組路徑對上手冊對 `MOV` 的定義 |
| `E_Rec` 實際欄位偏移是 `Env_Data`(+0)、`E_Vec`(+2)、`SIB`(+4) | 助手 `0x0fab` 用 `+2` 當 segment number 索引的陣列；`+4` 的 `+4`／`+6` 行為對上 `Ref_Count`／`Activity` |
| 程式碼段表頭：`+0x00` 程序字典 word 偏移、`+0x0c` byte sex、`+0x0e` 常數池 word 偏移 | 助手 `0x0fba` 尾段 |
| 程序字典往回長：`字典基底 − 2×程序號` | 呼叫助手 `0x101b` |
| `CXG` 的內嵌程序表 `0x1f56` 起 48 格，**33 格非零**，其中七對指同一位址 | 直接讀檔案位元組 |
| 遮罩表 `0x1fb6` 起 17 格，第 n 項 `(1 << n) − 1` | `LDP`／`STP` 的索引算法加表本身的內容 |
| big operand 解碼段（9 個位元組）在檔案裡出現 **19 次**，字面完全相同 | 位元組序列搜尋 |

### R2（2026-09-05）定名，M0 完成

- 專案定名 **Parhelion PME**，repo `Parhelion-PME86`，名字的由來寫在 `README.md`。
- 寫 [spec 01：codefile 靜態結構](docs/30-remake/specs/01-codefile.md)（狀態 `READY`），
  逐條配證據，並拿 `SYSTEM.PASCAL` 的實際位元組驗過。
- 實作 `internal/codefile` 與 `cmd/parhelion codefile`，**M0 通過**：
  28 個 segment、465 支常式、0 個字典項越界，兩筆 dictionary 記錄的鏈接得起來。
- 單元測試用合成的 codefile，不含原版資料；真檔驗收走 `PARHELION_CODEFILE` 環境變數。

這一輪解出來的東西：

| 結論 | 證據 |
|---|---|
| segment dictionary 記錄的 512 位元組版面（六個平行陣列＋`Next_Dict`＋`Copy_Note`＋`Sex`） | 最尾端的 `Copy_Note` 與 `Sex` 都讀得對，表示中間所有欄位大小加總正確 |
| `Seg_Info` 的 packed record 由低位往高位打包 | `KERNEL` 讀出 `0x8001` → 段號 1、`M_Psuedo`、版本 `IV`；反方向段號會是 128 |
| `Seg_Famly` 的變體記錄在 `Proc_Seg` 時是外層 unit 名 | `SEGFOPEN` 讀出 `"FILEOPS "`、`USERPROG` 讀出 `"KERNEL  "`，都對得到同檔案裡的段 |
| **`SYSTEM.PASCAL` 自己就混著兩種位元組序**：28 段裡 6 段的 byte sex 指示字讀出 256 | 直接讀檔案 |
| 常式版面是 `EXITIC`、`DATASIZE`、第一條指令，**字典項指的是中間的 `DATASIZE`** | 直譯器 `0x103a` 的 `mov cx, es:[di]` 後 `inc di; inc di`；430 支常式裡 418 支的 `EXITIC` 直接指到 `RPU`，0 支越界 |
| 段內所有段的 `REALSIZE` 都是 4 | 與 `LDCRL` 在堆疊上開 8 個位元組一致 |

spec 01 停在 `READY` 沒有升 `CONFORMED`。升級條件是開放項目 #8——
把原版放進 DOSBox 當 oracle 之前，「同狀態驗證」這個門檻沒有真的跨過。

### R3（2026-09-05）用 dosgolem 把原版跑起來

`dosgolem` 是同一個作者的無頭 DOS 執行器（為《大富翁2》的 remake 而寫），
可以當 Go 套件 import、決定性、看得到記憶體。拿它當 oracle 比 DOSBox 直接得多：
**問得到記憶體，不必隔著畫面反推。**

在 `dosgolem` 的一份副本上加了三樣東西：`.COM` 載入器、`int 16h` 的按鍵佇列、
IRQ1 與埠 `60h`。跑起來之後：

- p-System 開機到命令列，畫面上是 `[IV.2.1 R3.3]`——**版本從系統自己的畫面確認**，
  不再只是磁碟標示的推論。
- `PSYSTEM.VOL` 開得起來，`SYSTEM.PME.86` 定位在實體 `0x01400`（段 `0x0140`）。
- **開放項目 #1 消除**：映像偏移 `0x000`–`0x1ff` 與磁碟 `0x1d56` 起的 512 個位元組
  逐位元組相同。載入器把 dispatch 表搬到映像最前面，所以 `jmp word ptr cs:[di]`
  不必帶位移，而表項當成 `cs:offset` 剛好正確。
- 順帶把 #3 縮小：檔頭那張位址表就在被蓋掉的範圍內，所以讀它的一定是載入器。

這些量測**做成了會跑的測試**，不是一次性的觀察：`oracle/psys_test.go` 的
`TestLoaderMovesTheDispatchTableToOffsetZero` 每次都重驗 512／512。
`tools/ci.sh` 是本機 CI，需要素材的部分缺檔就跳過並且說明跳過了。

分工定了：**dosgolem 只放任何 DOS 程式都用得到的能力**（`.COM` 載入器、
`int 16h` 佇列、IRQ1 與埠 `60h`、讀文字畫面、用指紋在記憶體裡找映像），
它另外開了一個 `package dosgolem` 當對外介面；**認得 p-machine 的知識全部在
這個 repo 的 `oracle/`**。第一版把 p-System 專屬的東西寫進 dosgolem，
與那邊正在拆分的 `oracle/rich2` 是同一個問題，已經改掉。

### R4（2026-09-05）p-code 軌跡通了，spec 01 升 `CONFORMED`

鍵盤那條路先繞開：**開機過程本身就在跑 `SYSTEM.PASCAL` 的 p-code**，
不必等到命令列。`WaitForPME` 一小段一小段地跑，直譯器一出現就開始追。

追蹤的判準改成「剛執行的那一條是 `jmp word ptr cs:[di]`（`2E FF 25`），
而且落點是 dispatch 目標」。原本只看落點會多算——有些常式做完事直接跳進
另一支常式的入口（`SBR` 落進 `ADR` 就是），那一跳不是取指令。
改判準之後同一個 IPC 連續出現兩次的情形消失了。

軌跡讀起來就是 Pascal：

```
1251:0B08 23 SLDL4   1251:0AFA 21 SLDL2
1251:0B09 26 SLDL7   1251:0AFB 98 LDCN
1251:0B0A B2 LEQI    1251:0AFC B1 NEQI
1251:0B0B D5 FJPL    1251:0AFD D5 FJPL
```

左邊是迴圈條件，右邊是 `while p <> nil`。`FJPL` 在 `0B0B` 跳走、在 `0AFD`
落到 `0B00`（＋3 個位元組），兩種行為都對得上。

**同狀態驗證**（`oracle/conform_test.go`）：原版在跑的段，表頭段名讀出來是
`"USERPROG"`，讀取器從同一份檔案解出來的清單裡有它；段內容 99% 以上相同；
**400 條 p-code 的每一個 IPC，記憶體裡那個位元組與檔案裡同一個位移的位元組
逐一相同**。spec 01 因此升到 `CONFORMED`，並列出沒驗到的四項。

還沒通的：按鍵送進去之後，p-System 自己裝的 `int 09h`（`9CF0:01CA`）
沒有讀埠 `60h`。**不擋 M1**——要走到特定畫面時再解。

### R5（2026-09-05）M1 的對拍迴路成立，306 條逐條一致

寫了 Go 的 p-machine 核心（`internal/pmachine`）與對拍 harness（`oracle/parity.go`）。
**每一條指令的語意都從 8086 版直譯器的碼讀出來，手冊只當交叉檢查**；
規格在 [spec 02](docs/30-remake/specs/02-pmachine-core.md)（`CONFORMED`）。

對拍的形狀：從原版此刻的狀態抄一份（`Capture`），兩邊逐條走，
每一條之後比 `IPC`、`SP`、`TOS`。**停下來的三種情形分得出來**——
走完、有指令還沒實作、真的對不上。只有第三種是失敗；
第二種是進度，它會準確指出下一支要做的是哪一個。

迭代四輪，每輪補一批指令再跑：75 → 101 → 292 → **306 條**，
56 種 opcode，**0 個分歧**。停在跨段呼叫 `SCXG1`。

dosgolem 那邊補了兩塊通用能力：`Snapshot` 的型別別名（本來就是公開方法，
但回傳型別在 `internal/` 底下，使用端放不進自己的結構）與 `Machine.SegmentBytes`。

**對拍抓到的第一個 bug 值得記成規則。** 第 101 條的 `EFJ` 之後 IPC 差一個位元組：

```go
s.IPC = uint16(int16(s.IPC+1) + int16(int8(s.fetch())))   // 錯
```

Go 沒有規定 `a + b` 兩個運算元的求值順序，`s.IPC+1` 可能在 `fetch()` 已經推進
IPC **之後**才被讀到。四個位元組位移的跳躍全中，而**靜態看程式碼看不出來**——
它在某些編譯結果下是對的。**相對位址一律先把運算元取出來放進變數再算。**

### R6（2026-09-05）跨段呼叫完成，開放項目 #2 與 #12 消除

**`Seg_Base` 怎麼換算成段值，量出來了。** 靜態讀 @0x0fba 那段碼推不出來
（`0x1bee` 呼叫前 `ds` 被換成 `es`，看起來指標鏈跨了段），
但在跑起來的系統裡量一次就攤開了：

```
段值 ＝ paragraph(Codepool 基底) ＋ (池內偏移 ÷ 16)
```

`Seg_Base` 是 `SIB` 開頭兩個 word——**指向 Codepool 基底的指標（相對 `ss`）
與池內位元組偏移**，不是一個 20-bit 位址。八個段全部命中。
指標為 0 表示那一段在直譯器自己的段裡。零檢查看的是第二個 word。

順帶把 `SIB` 其餘欄位對完：`Seg_Name` 讀出 `"KERNEL  "`、`Seg_Leng` 讀出 2152，
與手冊 p.34 的宣告順序完全一致——只要把 `Seg_Base` 算成兩個 word。

實作了 `SCXG`／`CXL`／`CXG`／`CXI` 與跨段的 `RPU`。**`Environment` 這一層刻意
留在 `oracle/`，不放進 `pmachine`**：`E_Vec`／`SIB`／Codepool 是作業系統的資料結構，
換一個宿主就完全不同。

`dosgolem` 那邊補了觀測工具（`WatchWrite`／`WatchWord`／`BreakAt`／`RunUntil`／
`Insn`，也就是它 M3 掛著的那一格）。換段因此改成**監看直譯器的 `E_Rec`**，
不再輪詢——兩條 p-code 之間可以換過去又換回來，輪詢會漏掉，而漏掉不會報錯。
換了之後同一段軌跡從抽到 3 次變成收到 **45 次全部驗過**。

三種方式驗過：

| 驗什麼 | 怎麼驗 | 結果 |
|---|---|---|
| 機制 | 合成測資：呼叫、返回、內嵌程序要在切段前攔下、段不在記憶體 | 全過 |
| 段解析 | 我們算出來的 vs 原版切過去之後的暫存器 | **45 次換段逐一驗過，四項全同** |
| 端到端 | 把原版推到下一條就是真換段的地方再展開，逐條對拍 | **走過換段，`E_Rec D7D2 → 0664`** |

對拍現在停在**段 1 的內嵌原生程序**（程序 24）——那 26 支是宿主的機器碼，
與跨段機制是兩件事，錯誤訊息也分得出來。

### R7（2026-09-05）補測試：兩邊各項能力逐條釘住

之前的測試集中在「有沒有做出來」，缺的是「做錯了會不會被發現」。
補完之後 Parhelion 44 支、dosgolem 新增 16 支。

**Parhelion** 補的是**對拍走不到、或走到了也看不出來**的情形：
算術的運算元次序（加法顛倒結果相同）、`MODI` 的四種正負組合
（被除數為正時 Pascal 與 C 的 `%` 相同）、帶號與無號比較
（只用小正數測兩組都會過）、`IXA` 索引為 1 的捷徑、`LDM` 的推入方向、
位元組索引、靜態鏈層數、各種 fault，以及 codefile 反 byte sex 的常式解碼。

oracle 那一層補兩條根本前提：

- `Capture` 抄的八個基底逐一與直譯器自己的狀態變數比對。抄錯一個不會報錯，
  對拍會在幾條之後開始對不上，而症狀看起來像某條指令實作錯了。
- **軌跡是決定性的**：兩次開機的前 300 條 p-code 完全相同。
  不成立的話對拍會時好時壞，而看起來像被觀測的程式有問題。

**dosgolem** 補的是它當 oracle 的兩個前提，之前一支測試都沒有：
同一份輸入永遠得到同一個結果、快照真的是完整的狀態。
測資是會碰輪詢埠、記憶體與計時器的迴圈——只用 `nop` 迴圈測不出東西，
所以測試自己會檢查那些狀態真的動過。

順手修一個測試抓到的不一致：`WriteBytes` 原本繞過監看點，
所以「這塊資料是誰放的」少了宿主與載入器那一半。

### R8（2026-09-05）對拍從 306 條推到 8,665 條

先問「dosgolem 還缺什麼」，做法是**實際用它把剩下的路走完，看哪裡要自己重造輪子**。

結論是它已經夠用，但 Parhelion 沒有好好用它：`Trace` 自己手寫逐條 `Step` 的迴圈，
所以 dosgolem 的中斷點形同虛設。改成 `RunUntil` ＋ `Insn` 之後行為完全相同，
而且**白撿到一件事**——在直譯器的錯誤入口（映像偏移 `0x020f`）設中斷點，
原版自己跑進執行期錯誤時當場就知道。以前它會繼續跑，而我們拿一台已經出錯的
機器當對照，之後每一條都對不上，症狀看起來像我們的實作有問題。

**共同模擬**：宿主本來就該做的事（段 1 的內嵌原生程序、段還沒載入）交給原版走完，
再從它的狀態重抄一份繼續。放行的只有這兩種——p-code 指令沒實作**不算**，
那種也重抄的話「還沒做」會看起來像「做完了」。

接著補指令，一輪一輪往前推：

| 補了什麼 | 走到 |
|---|---|
| （共同模擬）| 321 |
| 位元組／字串比較、`XJP`、`LDC` | 325 |
| `INCI`、`LDE`／`LAE`／`STE`、`STM`、`MOV` | **8,665** |

最後那一跳是因為 `INCI` 卡在一個熱迴圈裡。**75 種 opcode、0 個分歧**，
另有 147 條交給原版自己走（全部是段 1 的內嵌原生程序）。停在 `LPR`。

### R9（2026-09-05）指令覆蓋率 190／210，對拍 8,782 條

盤了一次還差多少：210 個真的有實作的指令格裡，做完 **190**。

這一輪補的：

- **`LPR`**（擋住對拍的那一支）。它先把執行狀態同步進 TIB（@0x15f5），
  再讀一個 word；**負的暫存器號直接索引直譯器的狀態區**——
  原版寫成 `ss:[di+3Eh]`，−1 拿到 `ss:3Ch`（TIB 指標）、−2 拿到 `ss:3Ah`（E_Vec）。
- **整個集合家族**（`SRS`／`ADJ`／`INN`／`UNI`／`INT`／`DIF`／`EQPWR` 三支）。
  表示法是 N 個 word ＋ 頂端的 N，word k 在 `sp+2k`——由 `INN` 的
  `sp += 2k; pop` 讀出來。`ADJ` 會**拿掉長度**，之後那個集合就是定長的。
- **字串與陣列參數**（`CSP`／`CAP`／`ASTR`／`CSTR`）。描述符是兩個 word：
  E_Rec 與段內偏移，E_Rec 為 0 表示東西就在資料段。
- `CPF`（呼叫形式參數指的程序）、`SWAP`。

對拍 8,665 → **8,782 條**，86 種 opcode，0 個分歧。停在 `SIGNAL`。

剩下的 20 支分成兩堆，兩堆都不是「再補幾條指令」就能解決的：
浮點 16 支要先解出實數的位元格式（開放項目 #5）；
多工 4 支會換掉整個 task 的狀態，那是排程器。

### R10（2026-09-05）210／210，對拍走完整段開機

**浮點的格式量出來了：8 個位元組就是 IEEE 754 binary64，little-endian。**
助手 @0x22a8 把它拆開的方式一眼就看得出來——`and ax,0Fh` 取尾數高 4 位、
`and cx,7FF0h; shr 4` 取 11 位指數、`or al,10h` 補隱藏位元。
符號是最高位元組的 bit 7。開放項目 #5 消除。

**手冊 p.14 說實數是 BCD 浮點，這一版不是。** 1984 年 IEEE 754 已經是標準。
這一條同時是 `ucsd-pascal-notes` 摘譯的勘誤。

多工那四支：號誌的**非阻塞路徑就是加減一**，真的要排隊或喚醒才進排程器。
`NAT` 則 `retf` 跳進 8086 機器碼——結構上不是 p-machine 做得到的事，
與「還沒做」分成不同的錯誤型別。

於是 **210 個指令全部有實作**，四處依定義交給宿主：段 1 的內嵌原生程序、
`NAT`、段還沒載入、換 task。

對拍因此**走完整段開機**：

| | |
|---|---|
| 逐條一致 | **12,924 條 p-code** |
| 用到的 opcode | **130 種** |
| 分歧 | **0** |
| 交給原版自己走 | 191 條（`SCXG1`×184、`SIGNAL`×4、`WAIT`×3）|

停下來是因為**原版沒事做了**——開機跑完，系統停在等鍵盤的迴圈。
走到那裡表示整段工作量都對拍過了，那是收工，不是失敗。

### R11（2026-09-05）對拍走到 201,044 條，並且自己開得了機

**上一輪說「走完整段開機」是被預算誤導的。** 原版在開機後段會在兩條 p-code
之間花掉大量 8086 指令（等磁碟、輪詢鍵盤），每條 20 萬條的預算不夠，
`Trace` 回空，於是被判成「原版沒事做了」。預算提到 40 萬就繼續走，
並且立刻在第 16,887 條撞上真的分歧。

被找出來的五個都是真 bug：

| # | 是什麼 | 憑什麼 |
|---:|---|---|
| 1 | `EXIT` 拆框沒做——MSPROC 是負數時要跳 EXITIC，兩個位址取後面的那個 | @0x1160 的 `js` → @0x1175 |
| 2 | `SPR` 只重讀 MSPROC，該是整份狀態寫回 TIB 再整份讀回來 | @0x15F5／@0x1623 |
| 3 | `XJP` 的 case 表沒跟著 byte sex 走 | @0x0C12 |
| 4 | 換段沒把 byte sex 一起換掉 | @0x1002 |
| 5 | 直譯器狀態只留在 struct 裡沒寫回資料段 | 作業系統直接讀 `ss:110h` 的 activity 計數 |

第 5 個順帶解出 `MSIPC` 為 0 的意思（呼叫者是原生碼，@0x11A5 的 `lret`），
以及換段時 activity 與 `Ref_Count` 的維護（@0x10F5／@0x10DB／@0x113A）。

還有第六個：`LAE`／`LDE`／`STE` 不該查 SIB。它們碰的是**全域資料**不是程式碼，
助手 @0x15D2 只走 E_Vec → E_Rec → Env_Data 兩層；查 SIB 會讓「段不在記憶體」
變成一個假的理由把指令交出去。

**對拍因此走到 224,987 條逐條一致、173 種 opcode、0 個分歧。**

工具加了三樣，都是為了「分歧發生時看得出是誰弄壞的」：多比一項 E_Rec、
`-tail` 印出停下來之前的 24 步、分歧時把資料段逐位元組比一遍。
第 5 個 bug 就是資料段比對直接指出來的。

### R11b 自舉

`internal/psystem` 是宿主：平坦記憶體、`.VOL` 讀取器、開機時要擺好的資料結構、
段 1 的內嵌原生程序。**`cmd/boot` 不靠原版也不靠 DOS，直接從磁碟映像跑起來**，
第一條 p-code 與原版相同（`1AB8 CPL`），前 31 條逐條相同。

### R11c 裝置 I/O 打通，自舉走到 33,002 條

`UNITREAD`／`UNITWRITE` 是段 1 的程序 18／19（@0x2C5A／@0x2C5F），兩支只差一個
模式碼。**沒有靠讀碼猜，是量原版實際發出的呼叫**：進去前堆疊上那六個 word、
出來後緩衝區有沒有被填、`IORESULT` 是多少。讀與寫因此當場分得出來——
程序 18 讀到的正是磁碟目錄（`07 "PSYSTEM"`、`03E8` ＝ 1000 塊），
程序 19 寫出去的是 `<ESC>H`、`<ESC>E` 與版權字串。

連帶補上：資料段最上面由 `0xFFFE` 往下切三塊（目錄 4 塊、作業系統 codefile 的
segment dictionary 1 塊）、開機日期會蓋進**記憶體裡那份目錄**的第 0 筆 `+14h`、
把一段從磁碟讀進 codepool（程序 39）、relocation（程序 4）、
codepool ↔ 資料段的四支搬移、`IORESULT`／`IOCHECK`。

一個差一個位元組就全錯的地方：`SCXG` 存進 `ss:24h` 的 `si` 指著**程序號**，
原生程序多數會自己再存一次所以回到程序號後面，**沒有自己存的那幾支回到程序號
本身**，那個位元組會被當成下一條指令執行（程序 4 的 `04` ＝ `SLDC 4`）。

自舉因此走到 **33,002 條與原版逐條相同**，停在排程器：`SIGNAL` 要喚醒一個
在等的 task。細節與待解清單在 [spec 03](docs/30-remake/specs/03-boot.md)。

## 勘誤：被這一輪推翻的舊斷言

都在姊妹 repo [`ucsd-pascal-notes`](https://github.com/wicanr2/ucsd-pascal-notes)
的 `docs/30-opcode-tables/iv21-two-cpus.md` 與
`docs/50-iv-internals/instruction-set-details.md`，**同一輪已經回去改掉**
（commit `7891ccd`），並對該 repo 全部文件的「十進位（0x十六）」做過一次
一致性掃描，其餘 0 個不一致。

| 舊斷言 | 實際 | 證據 |
|---|---|---|
| 遮罩表是 `0x007f, 0x00ff, 0x01ff, 0x03ff…` | 表從 `0x1fb6` 起是 `0x0000, 0x0001, 0x0003…`；那串值要到 `0x1fc4` 才開始 | 直接讀檔案 |
| 「E_Rec 偏移 4 → `Env_Vect`，與手冊 p.37 宣告順序一致」 | `+2` 才是以 segment number 索引的指標陣列，`+4` 是 SIB | 助手 `0x0fab`、`0x15d2`、`0x10db` |
| `CXG` 節錄的 `cmp di, 2Fh` 之後直接 `mov di, cs:[di+1F56h]` | 中間還有 `and di, 0FFh` 與 `shl di, 1` | `0x13f9`–`0x1406` |
| 「8086 版只讀了 8 個常式，169 個相異目標是表的統計」 | 169 支全部反組譯到手，其中約三十支讀過並寫進文件 | `tools/routines-86.json` |
| 摘譯把 `RPU` 寫成「150（0x97）」 | 150 = `0x96`；dispatch 表 `0x96` 指向的就是 `RPU` | 十進位與十六進位換算 |

## 開放項目

每項都配一個消除條件——沒有消除條件的待辦會永遠留著。

| # | 問題 | 消除條件 |
|---|---|---|
| ~~1~~ | ~~`jmp word ptr cs:[di]` 沒有位移，表卻在檔案偏移 `0x1d56`~~ | **已消除（R3）**：載入器把磁碟 `0x1d56` 起的 512 個位元組搬到映像偏移 0，512/512 相同；`cs` 指著映像基底，所以表就在 `cs:0000` |
| ~~2~~ | ~~SIB 的確切版面與 `Seg_Base` 的算術~~ | **已消除（R6）**：在跑起來的系統裡量出來，八個段全部命中；指標相對 `ss` 不是 `es` |
| 3 | 檔頭 18 個 word 的用途。三格對上狀態變數偏移，其餘是程式碼位址 | 找到讀這張表的碼。**已知它在載入後被 dispatch 表蓋掉**（R3），所以讀它的一定是載入器 |
| 4 | 內嵌的 33 支機器碼程序分別做什麼 | 逐支反組譯並對上 IV.0 的 intrinsic 清單 |
| ~~5~~ | ~~8 位元組實數的位元格式~~ | **已消除（R10）**：IEEE 754 binary64，little-endian，由助手 @0x22a8 的拆解方式讀出來。手冊說的 BCD 不適用於這一版 |
| 6 | `MPR`（`0x24e1`）與 `DVR`（`0x262a`）被 80 條上限截斷 | 提高上限重跑，或改成追控制流而不是線性反組譯 |
| 7 | `ss:3Ch` 指向的記錄版面（堆疊檢查只用了 `+4`） | 找出誰設定 `ss:3Ch` |
| ~~8~~ | ~~把原版當 oracle：取得 p-code 軌跡~~ | **已消除（R4）**：軌跡取得出來，而且與 codefile 逐位元組對過 |
| 11 | 按鍵送進去之後 p-System 的 `int 09h` 沒有讀埠 `60h` | 反組譯 `9CF0:01CA` 那支處理常式，看它實際從哪裡拿鍵 |
| ~~12~~ | ~~跨段呼叫~~ | **已消除（R6）**：三種方式驗過，段解析五項全同 |
| 14 | 段 1 的 26 支內嵌原生程序 | 逐支反組譯出語意，對上 IV.0 的 intrinsic 清單（與開放項目 #4 同一件事）|
| ~~15~~ | ~~多工那 4 支~~ | **已消除（R10）**：非阻塞路徑做完；換 task 與 `NAT` 判定為宿主的工作，錯誤型別分開 |
| 16 | 換 task 的完整語意（ready queue、優先權、TIB 版面） | 要模型化多個 task 才驗得了，目前對拍碰到就交給原版 |
| 13 | 浮點的 8 位元組實數格式（開放項目 #5 的下游） | 同 #5 |
| 9 | 段頭第 9、10 個 word（手冊說「保留」）在樣本裡不是零，隨 segment 變化 | 找到寫它的東西（編譯器或 Linker），或在多份不同來源的 codefile 上看出規律 |
| 10 | 430 支常式裡有 12 支的 `EXITIC` 不是直接指到 `RPU` | 逐支反組譯那 12 個離開序列 |

## 下一輪候選

- **#14（內嵌原生程序）**：對拍就停在這一條。與開放項目 #4 是同一件事，
  解開它 M1 的可執行範圍會跳一大截。
- 接著補集合、字串、`MOV`／`STM` 這些不牽涉宿主的指令，用對拍逐個確認。
- #11（鍵盤）等到要走到特定畫面（跑編譯器、執行指定程式）時再解。

（以下是舊的候選，留著對照）

- **#8 先做。** 沒有 oracle，M1 的「兩邊輸出相同」沒有辦法驗，
  spec 01 也升不到 `CONFORMED`。
- 接著是 spec 02：常數池的內部結構（real pool 與 main pool 的夾層版面），
  M0 已經把常數池的位置解出來，只差內容。
- #4（33 支內嵌程序）與 #6（`MPR`／`DVR` 被截斷）是純反組譯工作，可以並行。
